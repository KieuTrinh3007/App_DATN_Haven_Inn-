<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">

    <!-- Màu nền cho hình tròn -->
    <solid android:color="#B2DAEC" />

    <!-- Độ rộng và màu của viền -->
    <stroke
        android:width="0dp"
        android:color="#FF8888" />

    <!-- Độ lớn của hình tròn -->
    <size
        android:width="40dp"
        android:height="40dp" />
</shape>
